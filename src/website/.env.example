Frontend public configuration is loaded from config/app-config.js.

Do not place backend-only values, database credentials, Firebase service-account credentials,
or server Google Maps keys in frontend configuration.
