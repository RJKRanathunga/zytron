Flask-Migrate/Alembic PostgreSQL migrations for the PolyLoop Flask API.

Run from src/server with the PG_* environment variables configured:

    flask db upgrade

The initial migration creates the explicit PostgreSQL table, constraint, and
index schema that matches the application models.
